node_modules/
.next/
out/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
__pycache__/
*.db
.claude/
.vite/
next-env.d.ts

# Local one-off dev/db scripts at repo root (NOT config .mjs files —
# postcss.config.mjs / eslint.config.mjs must stay tracked)
/bench.mjs
/check_passwords.mjs
/list_users.mjs
/test_pg.mjs
/test_pooler.mjs

# IDE / tool local state
.vscode/
.kilocode/
.roo/
.idea/

# Build info
*.tsbuildinfo

# Lockfiles — generated per-platform; committing a Windows lock breaks Linux
# native deps (e.g. @parcel/watcher) on the VPS. Let each machine resolve its own.
package-lock.json

# Stray copy of the landing repo (it lives in its own repo, not here)
/vyapar-landing-page/
/check_*.mjs
/test_*.mjs
